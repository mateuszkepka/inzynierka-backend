export enum MatchStatus {
    Scheduled = `scheduled`,
    Postponed = `postponed`,
    Finished = `finished`,
    Cancelled = `cancelled`,
    Resolving = `resolving`,
    Unresolved = `unresolved`
}
